# Theme Switcher Implementation

## Context
User wants multiple themes with a selector. First theme is "Midnight Blue" (already applied in CSS). Need to add theme switching capability.

## Work Objectives
- Add theme selector in topbar
- Persist theme selection in localStorage
- Apply theme class to body

## TODOs
- [ ] 1. Add theme selector component (ThemeToggle.tsx)
- [ ] 2. Add theme context/state management
- [ ] 3. Update layout to include theme toggle
- [ ] 4. Apply theme class to body element

## Final Verification
- [ ] F1. Theme dropdown appears in topbar
- [ ] F2. Clicking different theme changes colors immediately
- [ ] F3. Theme persists after page reload